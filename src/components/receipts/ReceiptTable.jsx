import { Link } from "react-router-dom";
import Card from "../common/Card";
import EmptyReceipts from "./EmptyReceipts";
import { formatCurrency } from "../../utils/currency";
import { getFinancialRouteToken } from "../../utils/financialIdentity";

function ReceiptTable({ receipts }) { return <Card><h2 className="text-xl font-semibold text-slate-800">Receipt History</h2>{receipts.length === 0 ? <EmptyReceipts /> : <div className="mt-4 overflow-x-auto"><table className="w-full min-w-[700px] text-sm"><thead className="border-b border-slate-200 text-left text-slate-500"><tr><th className="p-3">Receipt No.</th><th className="p-3">Invoice</th><th className="p-3">Client</th><th className="p-3">Date</th><th className="p-3">Method</th><th className="p-3 text-right">Amount</th></tr></thead><tbody>{receipts.map((receipt) => { const amount = Number(receipt.amount); return <tr key={getFinancialRouteToken(receipt) || receipt.receiptNumber} className="border-b border-slate-100"><td className="p-3">{getFinancialRouteToken(receipt) ? <Link to={`/receipt/${getFinancialRouteToken(receipt)}`} className="font-semibold text-blue-600 hover:underline">{receipt.receiptNumber || "Unnumbered receipt"}</Link> : <span>{receipt.receiptNumber || "Unnumbered receipt"}</span>}</td><td className="p-3">{receipt.invoiceNumber || "—"}</td><td className="p-3">{receipt.clientNameSnapshot || receipt.client || "—"}</td><td className="p-3">{receipt.date || "—"}</td><td className="p-3">{receipt.method || "—"}</td><td className="p-3 text-right">{formatCurrency(Number.isFinite(amount) && amount >= 0 ? amount : 0)}</td></tr>; })}</tbody></table></div>}</Card>; }
export default ReceiptTable;
