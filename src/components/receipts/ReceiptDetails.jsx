import Card from "../common/Card";
import Button from "../common/Button";

function ReceiptDetails({ receipt, company, onBack }) { return <Card className="p-8"><div id="receipt-print"><div className="flex flex-col justify-between gap-6 sm:flex-row"><div><h1 className="text-3xl font-bold text-slate-800">{company.name || "Company"}</h1><p className="text-slate-500">{company.tagline}</p><p className="mt-3 text-sm text-slate-600">{company.address}</p><p className="text-sm text-slate-600">{company.phone}</p><p className="text-sm text-slate-600">{company.email}</p></div><div className="sm:text-right"><h2 className="text-4xl font-bold text-slate-800">RECEIPT</h2><p className="mt-2 text-xl">{receipt.receiptNumber}</p></div></div><div className="my-8 grid gap-6 border-y border-slate-200 py-6 sm:grid-cols-2"><p><strong>Client</strong><br />{receipt.client}</p><p><strong>Invoice</strong><br />{receipt.invoiceNumber}</p><p><strong>Date</strong><br />{receipt.date}</p><p><strong>Method</strong><br />{receipt.method}</p><p><strong>Reference</strong><br />{receipt.reference || "—"}</p></div><div className="text-center"><p className="font-semibold text-slate-700">Amount Received</p><p className="mt-3 text-4xl font-bold text-emerald-600">GH₵ {(Number(receipt.amount) || 0).toLocaleString()}</p></div><p className="mt-8 text-center text-sm text-slate-500">Thank you for your business.</p></div><div className="mt-8 flex justify-end gap-3"><Button onClick={() => window.print()}>Print</Button><Button variant="secondary" onClick={onBack}>Back</Button></div></Card>; }
export default ReceiptDetails;
