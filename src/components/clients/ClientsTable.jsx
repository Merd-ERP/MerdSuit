import {useApp} from "../../context/AppContext";import Card from "../common/Card";import Button from "../common/Button";import EmptyClients from "./EmptyClients";function ClientsTable(){const{clients,setClients}=useApp();return <Card>{clients.length===0?<EmptyClients/>:<div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left"><th className="p-3">Name</th><th>Phone</th><th>Location</th><th>Email</th><th>Status</th><th>Actions</th></tr></thead><tbody>{clients.map(client=><tr key={client.id} className="border-b"><td className="p-3">{client.name}</td><td>{client.phone}</td><td>{client.location}</td><td>{client.email}</td><td>{client.status}</td><td><Button variant="danger" onClick={()=>setClients(items=>items.filter(item=>item.id!==client.id))}>Delete</Button></td></tr>)}</tbody></table></div>}</Card>}export default ClientsTable;
