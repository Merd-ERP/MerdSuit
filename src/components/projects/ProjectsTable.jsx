import { useApp } from "../../context/AppContext"; import Card from "../common/Card"; import Button from "../common/Button"; import EmptyProjects from "./EmptyProjects";
function ProjectsTable({ setProjectToEdit, onDelete }) { const { projects } = useApp(); return <Card>{projects.length === 0 ? <EmptyProjects /> : <div className="overflow-x-auto"><table className="w-full min-w-[800px] text-sm"><thead><tr className="border-b text-left"><th className="p-3">Project</th><th>Client</th><th>Location</th><th>Budget</th><th>Description</th><th>Status</th><th>Actions</th></tr></thead><tbody>{projects.map((project) => <tr className="border-b" key={project.id}><td className="p-3">{project.name}</td><td>{project.client || "—"}</td><td>{project.location}</td><td>{project.budget}</td><td className="max-w-48 truncate" title={project.description}>{project.description || "—"}</td><td>{project.status}</td><td><div className="flex flex-wrap gap-2"><Button variant="warning" onClick={() => setProjectToEdit(project)}>Edit</Button><Button variant="danger" onClick={() => onDelete(project)}>Delete</Button></div></td></tr>)}</tbody></table></div>};</Card>; }
export default ProjectsTable;
