import Card from "../common/Card";
import EmptyReport from "./EmptyReport";
function ReportTable({ report }) { return <Card><h2 className="text-lg font-semibold text-slate-800">{report.title}</h2><div className="report-table mt-4 overflow-x-auto">{report.rows.length === 0 ? <EmptyReport /> : <table className="w-full min-w-[680px] text-sm"><thead className="border-b border-slate-200 text-left text-slate-500"><tr>{report.headers.map((header) => <th key={header} className="p-3">{header}</th>)}</tr></thead><tbody>{report.rows.map((row, index) => <tr key={`${row[0]}-${index}`} className="border-b border-slate-100 hover:bg-slate-50">{row.map((cell, cellIndex) => <td key={cellIndex} className={`p-3 ${cellIndex === row.length - 1 && (report.title === "Sales Report" || report.title === "Payment Report" || report.title === "Expense Report" || report.title === "Inventory Report") ? "text-right" : ""}`}>{cell}</td>)}</tr>)}</tbody></table>}</div></Card>; }
export default ReportTable;
